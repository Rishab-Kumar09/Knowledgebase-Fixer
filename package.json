{
  "name": "knowledge-base-analyzer",
  "version": "1.0.0",
  "description": "Knowledge Base Article Analyzer",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "echo 'No build step needed - static files are already in public/'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "openai": "^4.0.0"
  }
} 